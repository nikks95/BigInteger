#include "BigInteger.h"
#include <conio.h>

int divide(int, int);
int main()
{
	/*
	BigInteger integer1, integer2;
	cin >> integer1 >> integer2;
	if (integer1 > integer2)
		cout << "greater";
	else
		cout << "less than";
		*/
	//int a, b;
	//cin >> a >> b;
	//cout << divide(a, b);
	getchar(); 
	return 0;
}